<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="interaction"
            type="com.car.sharing.ui.adapters.PostAdapter.PostInteraction" />

        <variable
            name="post"
            type="com.car.sharing.models.Post" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="10dp"
        android:layout_marginVertical="10dp"
        app:cardCornerRadius="5dp"
        app:cardElevation="5dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?android:attr/selectableItemBackground"
            android:clickable="true"
            android:focusable="true"
            android:onClick="@{() -> interaction.onPostSelected(post)}"
            android:padding="15dp">

            <ImageView
                android:id="@+id/prf_image"
                android:layout_width="25dp"
                android:layout_height="25dp"
                android:src="@mipmap/ic_launcher_round"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/user_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="10dp"
                android:text="@{post.ownerName}"
                android:textColor="#A9A9A9"
                android:textSize="12sp"
                app:layout_constraintBottom_toBottomOf="@+id/prf_image"
                app:layout_constraintStart_toEndOf="@+id/prf_image"
                app:layout_constraintTop_toTopOf="@+id/prf_image" />

            <ImageView
                android:id="@+id/car_image"
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:layout_marginVertical="10dp"
                android:src="@mipmap/ic_launcher"
                app:layout_constraintStart_toStartOf="@+id/prf_image"
                app:layout_constraintTop_toBottomOf="@+id/prf_image"
                app:mainPhoto="@{post.mainPhoto}" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/car_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="10dp"
                android:layout_marginTop="40dp"
                android:textSize="20sp"
                android:text="@{post.carName}"
                android:textStyle="bold"
                app:layout_constraintStart_toEndOf="@+id/car_image"
                app:layout_constraintTop_toTopOf="@+id/car_image" />

            <RatingBar
                android:id="@+id/ratingBar2"
                style="@style/Widget.AppCompat.RatingBar.Small"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="5dp"
                android:numStars="5"
                android:rating="@{Float.valueOf(String.valueOf(post.rateNumber))}"
                app:layout_constraintStart_toStartOf="@+id/car_name"
                app:layout_constraintTop_toBottomOf="@+id/car_name" />

            <com.google.android.material.textview.MaterialTextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="5dp"
                android:text="@{String.valueOf(post.rateNumber) + `/5`}"
                android:textColor="#A9A9A9"
                android:textSize="12sp"
                app:layout_constraintStart_toEndOf="@+id/ratingBar2"
                app:layout_constraintTop_toTopOf="@+id/ratingBar2" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/price"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text='@{"$" + String.valueOf(post.price) + `/day`}'
                app:layout_constraintBottom_toBottomOf="@+id/user_name"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="@+id/user_name" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.cardview.widget.CardView>


</layout>
